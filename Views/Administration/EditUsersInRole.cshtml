@model List<KalamYouthForumWebApp.Models.ViewModels.UserRoleViewModel>
@{
    ViewData["Title"] = "Manage User Roles";
    var roleID = ViewBag.roleId;
}


<form method="post">
    <div class="card">
        <div class="card-header">
            <h2>Add or Remove users from roles</h2>
        </div>
        <div class="card-body">
            @for (int i = 0; i < Model.Count; i++)
            {
                <div class="form-check m-1">
                    <input type="hidden" asp-for="@Model[i].UserId" />
                    <input type="hidden" asp-for="@Model[i].UserName" />
                    <input type="hidden" name="roleId" value="@roleID" />
                    <input asp-for="@Model[i].IsSelected" class="form-check-input" />
                    <label asp-for="@Model[i].IsSelected" class="form-check-label">
                        @Model[i].UserName
                    </label>
                </div>
            }


            @*@foreach (var user in Model)
            {
                <div class="form-check m-1">
                    <input type="hidden" asp-for="@user.UserId" />
                    <input type="hidden" asp-for="@user.UserName" />
                    <input type="hidden" name="roleId" value="@roleID" />
                    <input asp-for="@user.IsSelected" class="form-check-input" />
                    <label asp-for="@user.IsSelected" class="form-check-label">
                        @user.UserName
                    </label>
                </div>
            }*@
        </div>
        <div class="card-footer">
            <input type="submit" value="Update" class="btn btn-primary" />
            <a asp-action="Details" asp-controller="Administration" asp-route-id="@roleID" class="btn btn-secondary">Cancel</a>
        </div>
    </div>
</form>
